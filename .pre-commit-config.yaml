default_stages: [pre-commit]

repos:
  - repo: local
    hooks:
      - id: cubic-review
        name: cubic review
        entry: scripts/cubic_review.sh --merge-base origin/v1-of-regime-detection
        language: system
        pass_filenames: false
        stages: [pre-push]
