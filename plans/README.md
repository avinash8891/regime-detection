# Implementation Plans

Generated by the improve skill on 2026-06-18. Execute in the order below unless dependencies say otherwise. Each executor should read the selected plan fully before starting, honor STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Bind historical walk-forward to manifest-resolved paths | P1 | S | - | DONE |
| 002 | Replace production assert guards with explicit runtime errors | P2 | S | - | DONE |

## Dependency notes

- The two plans are independent.

## Vetted Findings

| # | Finding | Category | Impact | Effort | Risk | Evidence |
|---|---|---|---|---|---|---|
| 1 | Historical walk-forward materializes manifests but does not bind manifest-resolved paths before `run_walkforward`. | bug | A non-default manifest path can be downloaded and then ignored by classification. | S | MED | `scripts/run_historical_walkforward.py:543`, `scripts/run_historical_walkforward.py:561`, `scripts/run_historical_walkforward.py:568`; ADR at `docs/decisions/0010-per-label-hysteresis-and-audit-hardening.md:34`. |
| 2 | Production `assert` guards are used for runtime invariants. | security | `python -O` removes them, degrading explicit failures into later ambiguous errors. | S | LOW | `src/regime_data_fetch/acquisition_store.py:806`, `src/regime_detection/transition_risk_series.py:172`, `scripts/_v2_calibration_helpers.py:47`, plus `rg -n "^\\s*assert\\b" src scripts`. |

## Direction Findings

- No direction plans were written. The repo has active verification, manifest, and replay work; the useful next step from this audit is closing the two small correctness/tooling gaps above.

## Findings considered and rejected

- `spec_review.md` F-001/F-003 replay and macro archival gaps: already fixed in current code. `scripts/run_walkforward_replay_check.py` exists, `build_walkforward_report.py` binds replay dates/versions, and `run_historical_walkforward.py` passes `macro_series` to `write_archived_inputs`.
- `spec_review.md` F-002 transition-score model-instability renormalization: already fixed in current code. `compute_transition_score` raises `RuntimeError` when components are missing, and tests now cover missing model evidence.
- Ruff security output across `tests/`: rejected as planned work because pytest asserts are intentional and out of production scope.
- `deptry` and `vulture` findings: no current findings. `python3 -m deptry . --ignore DEP003` and `python3 -m vulture src scripts --min-confidence 90` exited 0.

## Audit scope

Audited recon docs, CI, manifest runner wiring, stale spec-review claims, production assert/security scan, dependency hygiene, and dead-code tooling. Not audited deeply: every classifier formula, every data-fetch parser, or live external data behavior.
